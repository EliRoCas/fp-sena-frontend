import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sigef.app',
  appName: 'sigef-final-proyect',
  webDir: 'dist/sigef-final-proyect/browser'
};

export default config;
